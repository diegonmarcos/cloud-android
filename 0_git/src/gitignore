# ══════════════════════════════════════════════════════════
# REPO: cloud (PUBLIC)
# https://github.com/diegonmarcos/cloud-infra
# ══════════════════════════════════════════════════════════

# ── Secrets & Credentials (reinforces global gitignore) ──
# KEEP IN SYNC WITH:
#   Pre-commit hook: 0_git/src/hooks/pre-commit  (blocks even git add -f)
#   Global gitignore: ~/git/cloud-unix/ba_flakes_desktop/src/modules/programs/git.nix
#   Global gitignore: ~/git/cloud-unix/bb_flakes_termux/src/modules/programs/git.nix
## Decrypted secrets output
.secrets
*.secrets
**/.secrets
.secrets.d/
**/.secrets.d/
## .secrets.json — JSON dumps of decrypted env (build engine output).
## NOT matched by *.secrets glob (which only matches files literally
## ending in .secrets, not .secrets.json). Found 2026-05-05 leaking
## AUTHELIA_CLI_SECRET / CF_API_TOKEN / NTFY_ADMIN_AUTH from dist/.
.secrets.json
*.secrets.json
**/.secrets.json
**/dist/.secrets.json
## NOTE: secrets.yaml (+ .new / .draft / .bak variants) are ALLOWED
## to be tracked — but ONLY when sops-encrypted. The pre-commit hook's
## content-scan blocks any secret-named file lacking `ENC[AES...]` or
## `sops:` markers. Fix workflow: sops -e -i secrets.yaml → commit OK.
## Environment files
.env
.env.*
**/.env
**/.env.*
!.env.example
!**/.env.example
## Private keys & certificates
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
## Auth tokens & credentials
*.token
*credentials*.json
*_token.json
*_tokens.json
## Cloud provider configs
.oci/
.gcloud/
## GPG & age
*.asc
*.age
!*.age.pub
## Terraform secrets
terraform.tfvars
*.tfvars
!*.tfvars.example

# ── Dependencies (managed by shared ~/.node_modules) ──
node_modules

# ── Repo-specific ──
## Terraform
.terraform/
*.tfstate
*.tfstate.backup
!*.tfstate.enc
## Nix build artifacts
result
.result
## Temporary files
*.tmp
## Generated data files → symlinks to cloud-data/ submodule (tracked)
## Obsidian
.obsidian/workspace-mobile.json

# Deployed secrets (never commit)
b_infra/nixhm-sudo-*/dist/.secrets
b_infra/nixhm-sudo-*/dist/.secrets.d/

# ── Nix closure binaries (generated by HM docker-package step) ──
*.nar.gz
**/nix-store/
**/nix-path-info.json.gz

# ── Vendored Python deps (regenerated at build time) ──
**/.deps/

# ── Rust build artifacts (regenerated at build time) ──
**/target/
**/.cargo-home/

## Per-service build transcript written by cloud-ship-container-engine.sh
## (tee -a "$BUILD_LOG_FILE"). Purely local run output — 65 of these staged
## themselves the first time every service build.sh was exercised.
build.log
**/build.log

# ── Agent/editor machine state ───────────────────────────────────────────────
## These sit inside directories that ARE committed (.claude/, .obsidian/), so
## they need naming individually — the deploy step already treats them as
## "machine state, never managed" and refuses to overwrite them, but nothing
## stopped them being committed. vault and tools each shipped a
## settings.local.json full of one machine's Bash permission grants, including
## absolute paths into a repo layout that no longer exists.
##
## Committed alongside, deliberately: .claude/settings.json (shared config) and
## .mcp.json (generated, identical everywhere).
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude.json
.claude.json.backup
.claude.json.tmp.*
.obsidian/workspace.json
.obsidian/workspace-mobile.json

## ─── Android app trees (moved from cloud-unix 2026-08-25, ea_* -> aa_*) ───
## EXCEPTION: aa_cloud-superapp/libs/ is the gradle multi-module source tree
## renamed from ea_superapp → aa_cloud-superapp 2026-05-30.
!aa_cloud-superapp/libs/
!aa_cloud-superapp/libs/**
## EXCEPTION: aa_cloud-wallet/libs/ is the gradle multi-module source tree
## (libs:wallet). Same pattern as aa_cloud-superapp/libs/. Added 2026-07-12.
!aa_cloud-wallet/libs/
!aa_cloud-wallet/libs/**
## EXCEPTION: aa_cloud-nav/libs/ is the gradle multi-module source tree
## (libs:core, libs:maps, libs:updater). Same pattern as aa_cloud-superapp/libs/.
!aa_cloud-nav/libs/
!aa_cloud-nav/libs/**
## EXCEPTION: aa_cloud-calendar/libs/ is the gradle multi-module source tree
## (libs:core, libs:updater, libs:cal). Same pattern as aa_cloud-superapp/libs/.
!aa_cloud-calendar/libs/
!aa_cloud-calendar/libs/**
## EXCEPTION: aa_cloud-news/libs/ is the gradle multi-module source tree
!aa_cloud-news/libs/
!aa_cloud-news/libs/**
!aa_cloud-contacts/libs/
!aa_cloud-contacts/libs/**
## EXCEPTION: aa_cloud-vault/libs/ — the FIFTH time this blanket rule has
!aa_cloud-vault/libs/
!aa_cloud-vault/libs/**
## EXCEPTION: aa_cloud-media-center/upstream/libs/ — the SIXTH time. This one
## as !aa_cloud-media-center/libs/ would not have matched it.
!aa_cloud-media-center/upstream/libs/
!aa_cloud-media-center/upstream/libs/**
aa_cloud-superapp/**/*.keystore
aa_cloud-superapp/**/*.jks
ea_cloud-comms/**/*.keystore
ea_cloud-comms/**/*.jks
aa_cloud-ide/**/*.keystore
aa_cloud-ide/**/*.jks
aa_cloud-nav/**/*.keystore
aa_cloud-nav/**/*.jks
## Bitwarden Android for aa_cloud-vault's `vault-bitwarden` fork, added
## aa_cloud-<app>/patches/ (tracked). Was covered by the old `ea_*-*/`
aa_upstreams-sources/
aa_cloud-wallet/**/*.keystore
aa_cloud-wallet/**/*.jks
aa_cloud-browser/**/*.keystore
aa_cloud-browser/**/*.jks
aa_cloud-vault/**/*.keystore
aa_cloud-vault/**/*.jks
aa_cloud-contacts/**/*.keystore
aa_cloud-contacts/**/*.jks
aa_cloud-media-center/**/*.keystore
aa_cloud-media-center/**/*.jks
## `ea_*-*/` blanket rule removed 2026-07-10; 93fdc30c re-added explicit ignores
## that were later lost. If any becomes a real app, whitelist it like aa_cloud-*.
aa_keyboard-dicts/
aa_keyboard-heliboard/
aa_net-firestack/
aa_zoomies-pets/
aa_mail-fairmail/
aa_cal-davx5/
aa_feed-feeder/
aa_net-wireguard/
aa_vault-keepassdx/
aa_net-kdeconnect/
aa_net-rethinkdns/
## Re-ignore Gradle build outputs inside the whitelisted aa_cloud-ide/ tree
## (the !aa_cloud-ide/** whitelist would otherwise track build/.gradle dirs).
aa_cloud-ide/.gradle/
aa_cloud-ide/build/
aa_cloud-ide/**/build/
aa_cloud-nav/.gradle/
aa_cloud-nav/build/
aa_cloud-nav/**/build/
aa_cloud-media-center/.gradle/
aa_cloud-media-center/build/
aa_cloud-media-center/**/build/
## aa_cloud-calendar was added after this list and never got its entries, so a
## ~3M-line aa_cloud-superapp cache commit (264c09f2). Added 2026-08-09.
aa_cloud-calendar/.gradle/
aa_cloud-calendar/build/
aa_cloud-calendar/**/build/
aa_cloud-calendar/.cache/
aa_cloud-calendar/.kotlin/
## Same for aa_cloud-news. These MUST sort after the !aa_cloud-news/libs/**
aa_cloud-news/.gradle/
aa_cloud-news/build/
aa_cloud-news/**/build/
## Same for aa_cloud-vault — MUST sort after its whitelist above.
aa_cloud-vault/.gradle/
aa_cloud-vault/build/
aa_cloud-vault/**/build/
aa_cloud-vault/.cache/
aa_cloud-vault/.kotlin/
aa_cloud-news/.cache/
aa_cloud-news/.kotlin/
aa_cloud-contacts/.gradle/
aa_cloud-contacts/build/
aa_cloud-contacts/**/build/
aa_cloud-contacts/.cache/
aa_cloud-contacts/.kotlin/
## aa_cloud-calendar's first local ./gradlew run surfaced it, but the gap
## Re-ignore Gradle/Kotlin daemon caches inside the whitelisted aa_cloud-superapp/
aa_cloud-superapp/.cache/
aa_cloud-superapp/.kotlin/
## !ea_cloud-*/** whitelists would otherwise make a locally-built APK / dist/
aa_cloud-superapp/dist/
aa_cloud-superapp/**/*.apk
ea_cloud-comms/dist/
ea_cloud-comms/**/*.apk
aa_cloud-ide/dist/
aa_cloud-ide/**/*.apk
aa_cloud-nav/dist/
aa_cloud-nav/**/*.apk
aa_cloud-wallet/dist/
aa_cloud-wallet/**/*.apk
aa_cloud-browser/dist/
aa_cloud-browser/**/*.apk
aa_cloud-vault/dist/
aa_cloud-vault/**/*.apk
aa_cloud-media-center/dist/
aa_cloud-media-center/**/*.apk
aa_cloud-calendar/dist/
aa_cloud-calendar/**/*.apk
