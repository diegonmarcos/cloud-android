# Nothing reflective here; the module is plain HttpURLConnection + a Kotlin
# object. Kept so the module matches the shape of the other shared libs and
# has somewhere to put a rule the day one is needed.
